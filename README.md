# ENCRYPT-YOUR-FILES
this tool is for people with trust issues such as me i created this tool so anyone could use it to encrypt their own files by them selves! 
the tool is free simple and works perfectly it uses the standard AES265 GCM encryption algorithm (IT ALSO HASHS YOUR MESSAGE!) just install a coupel of libraries and it should be ready to work
benefits: 
1. free
2. no internet
3. simple and very effective
4. very secure and uses simple code no complications (GOOD LUCK TO HACKERS TRYING TO BREAK THE TOOL)
5. generates key, encrypts, decrypts and hashs
6. the key is generated from a highly random number
7. the key can be written on a paper (if your attack model doesnt include the attacker breaking into your house and stealing the key) since its decoded once its generated then the tool encodes it for encryption/decryption
8. anyone can operate it
9. works on txt files, pdf, small videos, music files and basiclly any small size file

how to use it:

take the files install the libraries then open the source code in two different windows then create a dummy file first to encrypt and understand how the tool works copy the file path 
run the tool choose 1. generate key wirte it down or copy it temporarly somewhere else then run the tool again choose 2. encrypt the tool will ask you to enter the file path and the key do so after that it will generate the ciphertext now we wanna decrypt you have the key, cipher text and the tool rerun the tool on the second window you opened earlier choose 3. decrypt enter the file path and the key then boom open your file its now decrypted 

NOTE: this tool is only for educational purposes using this tool for malcious acts legal actions will be taken against you this tool is not for commercial use  
